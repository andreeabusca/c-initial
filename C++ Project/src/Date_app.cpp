#include "Date_app.h"

Date_app::Date_app(int day, int month, int year, int hour, int minute):day(day),month(month),year(year),hour(hour),minute(minute)
{
    //ctor
}

Date_app::~Date_app()
{
    //dtor
}
